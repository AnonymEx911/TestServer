import { IOptions } from './interfaces/options.interface';
import { IServer } from './interfaces/server.interface';
declare class RageMP {
    private options;
    constructor(options: IOptions);
    getServers(): Promise<IServer[]>;
    getSingleServer(ip: string): Promise<IServer | undefined>;
    getServersByGamemode(gamemode: string): Promise<IServer[]>;
    getServersByLang(lang: string): Promise<IServer[]>;
}
export default RageMP;
